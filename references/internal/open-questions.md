# Open questions

1. Is Storybook consistently exposed as a Gem, or only by prompt trigger on some accounts?
2. Which DOM markers are most stable for generation progress and completed-book detection?
3. Is share-link extraction available directly in the UI, or only behind an overflow/share modal?
4. Which PDF export path is more stable on this host: Playwright `page.pdf()` on a printable route, or print-dialog driven export via Chromium kiosk options?
5. What account/session-expired signals appear first, URL redirects, avatar/login button changes, or modal prompts?
